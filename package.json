{
  "name": "crdas",
  "version": "1.0.0",
  "description": "CRDAS Application Architecture",
  "main": "server.js",
  "scripts": {
    "build": "cd frontend/result-app && npm install && npm run build && cd ../weather-app && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "express": "^5.2.1"
  }
}
